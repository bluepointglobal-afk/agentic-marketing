{
  "name": "agentic-marketing",
  "version": "0.1.0",
  "private": true,
  "description": "Multi-brand content pipeline: Next.js cockpit + standalone Agent SDK worker.",
  "workspaces": [
    "packages/*",
    "apps/*",
    "worker"
  ],
  "scripts": {
    "dev": "concurrently -n web,worker -c blue,green \"npm run dev:web\" \"npm run dev:worker\"",
    "dev:stub": "concurrently -n web,worker -c blue,green \"npm run dev:web\" \"PIPELINE_DRIVER=stub npm run dev:worker\"",
    "dev:web": "npm run dev --workspace apps/web",
    "dev:worker": "npm run dev --workspace worker",
    "seed": "npm run seed --workspace worker",
    "build:web": "npm run build --workspace apps/web",
    "build:worker": "npm run build --workspace worker",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "infra:up": "docker compose up -d",
    "infra:down": "docker compose down"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  },
  "engines": {
    "node": ">=20"
  }
}
